name: First Workflow
on: 
  push:
    branches: 
      - main
  workflow_dispatch:

jobs:
  FirstJob:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World!"
      - run: echo "::error Erreur!"
      - run: echo "::warning Warning!"

      - name: Checkout
        uses: actions/checkout@v4.2.2
      - name: List files in repo
        run: |
          echo "Repo ${{ github.repository }} contains the following - files:"
          tree
        
    
